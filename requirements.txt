# Mirrors [project.dependencies] in pyproject.toml, which is what bench reads.
# oan_auth_service is not listed here: it is a Frappe app, declared in hooks.py
# as required_apps and installed from its repo rather than from PyPI. PyJWT is
# that app's own dependency; this one does not import it.

# Everything this app imports at runtime is listed, even what Frappe ships: each
# guards a boundary, and an import that vanishes with a Frappe upgrade would fail
# open or fail at request time rather than at install.

# Request schemas and per-field validation errors (api/v1, services/identity).
pydantic>=2.0

# Attachment pipeline: magic-byte sniff, EXIF/GPS strip, PDF structure check.
filetype>=1.2
Pillow>=10.0
pypdf>=4.0

# Streams attachment bytes as a raw Response from the view route (api/v1/attachment).
Werkzeug>=3.0
