# Mirrors [project.dependencies] in pyproject.toml, which is what bench reads.
# oan_auth_service is not listed here: it is a Frappe app, declared in hooks.py
# as required_apps and installed from its repo rather than from PyPI. PyJWT is
# that app's own dependency; this one does not import it.

# Attachment pipeline. Both ship with Frappe today, but this module imports them
# directly and each guards a security boundary -- the magic-byte sniff and the
# EXIF strip fail open if the import disappears, so neither is left transitive.
filetype>=1.2
Pillow>=10.0
# Same pattern: Frappe ships phonenumbers, but we import it for contact_mobile.
phonenumbers~=9.0.23
# Request schemas and per-field validation errors (api/v1, services/identity).
pydantic>=2.0
# PDF structure check in the attachment pipeline (services/scanning).
pypdf>=4.0
# Streams attachment bytes as a raw Response from the view route (api/v1/attachment).
Werkzeug>=3.0
